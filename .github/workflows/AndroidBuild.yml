name: AndroidBuild
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0

      - name: Setup Java JDK
        uses: actions/setup-java@v3.13.0
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Grant execute permission for gadlew
        run: chmod +x ./gradlewg

      - name: Build With Gradle
        run: ./gradlew build

      - name: Upload  Build Artifact
        uses: actions/upload-artifact@v3.1.3
        with:
          name: MovieApp.apk
          path: app/builds/outputs/apk/debug/app-debug.apk

